<%= form_for(@kind_transaction) do |f| %>
  <% if @kind_transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kind_transaction.errors.count, "error") %> prohibited this kind_transaction from being saved:</h2>

      <ul>
      <% @kind_transaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
